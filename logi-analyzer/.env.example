# Neon Postgres (Vercel) — довідкові дані (тарифи/коди/санкції/рулбук)
DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require"

# Єдина «поточна дата» для промптів AI (ISO). Якщо не задано — серверна дата.
CURRENT_DATE=""

# AI-ключі: BYOK на клієнті (localStorage). Тут НЕ зберігаються.
# Проксі-роут /api/ai приймає {provider, apiKey} на кожен запит.
