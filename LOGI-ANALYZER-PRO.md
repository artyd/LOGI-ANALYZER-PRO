# LOGI-ANALYZER PRO · Freight Intelligence Terminal

> **Версія:** 2.1  
> **Дата:** © 2026  
> **Мова інтерфейсу:** Українська  
> **Тип додатку:** Single-page web application (SPA)  
> **Файл:** `index.html` (~4600 рядків, повністю самодостатній)

---

## Зміст

1. [Огляд проєкту](#1-огляд-проєкту)
2. [Призначення та цільова аудиторія](#2-призначення-та-цільова-аудиторія)
3. [Технологічний стек](#3-технологічний-стек)
4. [Архітектура додатку](#4-архітектура-додатку)
5. [Інтерфейс та UX/UI](#5-інтерфейс-та-uxui)
6. [Сторінки та навігація](#6-сторінки-та-навігація)
7. [Система API-ключів та провайдерів](#7-система-api-ключів-та-провайдерів)
8. [Процес аналізу вантажу](#8-процес-аналізу-вантажу)
9. [Обробка даних — вибір актуального листа](#9-обробка-даних--вибір-актуального-листа)
10. [AI-аналіз та системний промпт](#10-ai-аналіз-та-системний-промпт)
11. [Safety-фільтр після AI](#11-safety-фільтр-після-ai)
12. [Рендеринг результатів](#12-рендеринг-результатів)
13. [Експорт звіту в XLSX](#13-експорт-звіту-в-xlsx)
14. [Архів перевірок](#14-архів-перевірок)
15. [База знань ЗЕД](#15-база-знань-зед)
16. [Теми оформлення](#16-теми-оформлення)
17. [Адаптивність (Responsive)](#17-адаптивність-responsive)
18. [Зберігання даних](#18-зберігання-даних)
19. [Зовнішні залежності](#19-зовнішні-залежності)
20. [Обмеження та відомі особливості](#20-обмеження-та-відомі-особливості)
21. [Структура коду](#21-структура-коду)
22. [Changelog v2.1](#22-changelog-v21)

---

## 1. Огляд проєкту

**LOGI-ANALYZER PRO** — це веб-додаток для аналізу збірних вантажних партій фармацевтичної та хімічної продукції, що транспортуються з Китаю/Індії в Україну транзитом через порти Євросоюзу (Гамбург, Роттердам, Гданськ). Додаток використовує AI (через зовнішні API) для аналізу маніфестів вантажів і автоматичного розрахунку митних платежів, оцінки ризиків, підготовки чекліста документів та перевірки відповідності нормам ЄС та України.

Весь додаток міститься в єдиному HTML-файлі (~4600 рядків) і не потребує бекенду — працює повністю на стороні клієнта (client-side).

**Ключова особливість v2.1:** при аналізі файлу з кількома листами система автоматично вибирає **один актуальний лист** поточного збору контейнера і передає в AI виключно його рядки. Всі інші листи (старі збірки, субстанції, архіви, розрахунки) повністю ігноруються ще до відправки даних в AI.

---

## 2. Призначення та цільова аудиторія

**Цільова аудиторія:** логісти, митні брокери, спеціалісти ЗЕД (зовнішньоекономічної діяльності), імпортери фармацевтичної та хімічної продукції.

**Ключові задачі, які вирішує додаток:**

- Аналіз вмісту збірної партії з Excel/CSV файлів або Google Sheets
- Автоматичний вибір актуального листа поточного збору з багатолистових файлів
- Класифікація товарів за категоріями (АФІ, ветеринарна АФІ, косметична сировина, харчова добавка, допоміжна речовина, промислова хімія, подвійне призначення)
- Розрахунок митних платежів: митна вартість (CIF), мито, ПДВ 20%
- Оцінка ризиків для кожного товару (Критичний / Середній / Низький)
- Формування чекліста перевірок на митниці ЄС (фізичні та документальні)
- Формування чекліста розмитнення на митниці України
- Аналіз небезпечних вантажів (ADR-класифікація)
- Перевірка санкційних обмежень ЄС
- Генерація XLSX-звіту для завантаження
- Довідкова база знань з митного законодавства ЄС та України

---

## 3. Технологічний стек

| Компонент | Технологія |
|-----------|------------|
| Frontend | Vanilla HTML/CSS/JavaScript (без фреймворків) |
| Шрифти | Google Fonts: JetBrains Mono, Instrument Sans, Instrument Serif |
| Парсинг Excel | SheetJS (XLSX) — завантажується динамічно з CDN при потребі |
| AI-провайдери | OpenAI (GPT-4o), Google Gemini (1.5 Pro), Anthropic Claude (opus-4-7), OpenRouter |
| Зберігання | localStorage (API-ключі, архів, тема, історія) |
| Експорт | SheetJS для генерації XLSX-файлів |

---

## 4. Архітектура додатку

Додаток побудований як **монолітний SPA** з такою логічною структурою:

```
index.html
├── <head>
│   ├── Метадані та Google Fonts
│   └── <style> — повна CSS-система (~2400 рядків)
│       ├── CSS Custom Properties (змінні)
│       ├── Темна тема (за замовчуванням)
│       ├── Світла тема (.theme-light)
│       ├── Topbar
│       ├── Навігація
│       ├── Command Deck (головна секція вводу)
│       ├── API Key Card (flip-анімація)
│       ├── Key Management Modal
│       ├── Таблиці результатів
│       ├── Метрики, алерти, пілюлі ризиків
│       ├── Loader
│       ├── Knowledge Base (zed-cards)
│       ├── Архів
│       ├── Footer
│       └── Responsive (@media)
├── <body>
│   ├── Topbar (бренд, навігація, тема, API-чіп)
│   ├── Сторінка «Новий аналіз» (#page-new)
│   │   ├── API Key Card (flip-картка)
│   │   ├── Command Deck (URL-ввід, файл-аплоад)
│   │   └── Results Area (динамічний контент)
│   ├── Сторінка «Архів» (#page-arch)
│   ├── Сторінка «База знань» (#page-zed)
│   ├── Footer
│   └── Key Management Modal
└── <script> — повна JS-логіка (~2200 рядків)
    ├── Управління провайдерами/ключами
    ├── Універсальний AI-caller (callAI)
    ├── Навігація між сторінками
    ├── Завантаження та парсинг файлів
    ├── parseSheetDate() — парсинг дати з назви листа
    ├── findDataHeader() — пошук заголовка товарної таблиці
    ├── selectActualSheet() — вибір одного актуального листа ← NEW
    ├── Фільтрація «сміттєвих» рядків
    ├── Нормалізація та дедублікація товарів
    ├── Батч-аналіз через AI (тільки з обраного листа)
    ├── Safety-фільтр відповіді AI ← NEW
    ├── Розрахунок митних платежів
    ├── Рендеринг результатів + банер листа ← UPDATED
    ├── Генерація XLSX-звіту (з метаданими листа) ← UPDATED
    ├── Архів (localStorage, з назвою листа) ← UPDATED
    ├── База знань (zedTopics)
    └── Утиліти (годинник, форматування)
```

---

## 5. Інтерфейс та UX/UI

### Дизайн-система

Додаток використовує **«Control Room»** естетику — темний інтерфейс із зеленим акцентом, що нагадує промислову панель управління або термінал.

**Кольорова палітра (Dark theme):**

| Змінна | Колір | Призначення |
|--------|-------|-------------|
| `--bg` | `#0a0b0d` | Основний фон |
| `--surface` | `#111317` | Поверхні карток |
| `--amber` | `#2cb878` | Основний акцент (зелений) |
| `--ink` | `#e8ebef` | Основний текст |
| `--green` | `#2cb878` | Статус «OK» |
| `--yellow` | `#e5b53a` | Статус «Увага» |
| `--red` | `#ef4a4a` | Статус «Критично» |

**Типографіка:**

| Змінна | Шрифт | Використання |
|--------|-------|-------------|
| `--mono` | JetBrains Mono | Код, технічні дані, бейджі, мітки, назва листа |
| `--sans` | Instrument Sans | Основний текст, навігація, кнопки |
| `--serif` | Instrument Serif | Заголовки сторінок, заголовки карток |

**Декоративні елементи:**

- Фонова сітка 48×48px з mask-image (радіальний градієнт) та низькою прозорістю
- Ambient glow — радіальні градієнти зеленого відтінку по кутах
- Кутові декоративні рамки на Command Deck (L-подібні кути зеленим кольором)
- Пульсуючі індикатори (анімація `pulse-ok`, `pulse-amber`)
- Topbar із `backdrop-filter: blur(20px)` — «матове скло»
- **Інформаційний банер обраного листа** — зелена смуга під заголовком результатів із назвою листа, датою, причиною вибору та переліком проігнорованих листів

---

## 6. Сторінки та навігація

Додаток має **3 сторінки** з навігацією через кнопки у верхній панелі:

### 6.1. Новий аналіз (`#page-new`)

Головна сторінка з полем вводу URL Google Sheets або завантаженням файлу. Містить:

- **API Key Card** — flip-картка для початкового вибору провайдера та введення ключа (зникає після збереження)
- **Command Deck** — центральна секція з заголовком, описом та формою вводу
- **Input Row** — поле URL + кнопка «Запустити аналіз»
- **File Upload** — альтернативне завантаження Excel/CSV файлу
- **Results Area** — область для динамічного відображення результатів, що починається з **банера обраного листа**

### 6.2. Архів (`#page-arch`)

Історія попередніх аналізів, збережена в localStorage (до 50 записів). Кожен запис відображає джерело, дату, назву обраного листа, кількість позицій, загальну суму та рівень ризику. Клік по запису відкриває збережені результати.

### 6.3. База знань (`#page-zed`)

Довідковий розділ із 8+ тематичними картками з інформацією про митне законодавство. Кожна картка відкривається у розгорнутому вигляді з розділами та парами ключ-значення.

---

## 7. Система API-ключів та провайдерів

### Підтримувані провайдери

| Провайдер | Модель | Формат ключа | Ендпойнт |
|-----------|--------|-------------|----------|
| OpenAI | `gpt-4o` | `sk-proj-...` | `api.openai.com/v1/chat/completions` |
| Gemini | `gemini-1.5-pro` | `AIzaSy...` | `generativelanguage.googleapis.com/v1beta/...` |
| Claude | `claude-opus-4-7` | `sk-ant-...` | `api.anthropic.com/v1/messages` |
| OpenRouter | `openai/gpt-4o` | `sk-or-...` | `openrouter.ai/api/v1/chat/completions` |

### Потік управління ключами

1. **Початковий стан** — видно flip-картку з вибором провайдера
2. **Вибір провайдера** — картка перевертається (CSS 3D-transform), з'являється поле вводу ключа
3. **Збереження ключа** — картка зникає (анімація `fadeOut`), ключ зберігається в localStorage
4. **Зміна ключа** — через модальне вікно (кнопка API-чіп у topbar)

### Функція `callAI(systemPrompt, userPrompt, maxTokens, json)`

Універсальна обгортка над 4 API. Автоматично:
- Вибирає ендпойнт та формат запиту для поточного провайдера
- Встановлює JSON-mode (де підтримується)
- Передає `anthropic-dangerous-direct-browser-access: true` для Claude
- Повертає текст відповіді або кидає помилку з описом

---

## 8. Процес аналізу вантажу

### Потік (`runAudit()`)

```
Ввід URL / файл
    ↓
Завантаження даних (fetch XLSX або file reader)
    ↓
SheetJS: парсинг всіх листів → sheets[]
    ↓
parseSheetDate() + findDataHeader() для кожного листа
    ↓
selectActualSheet() → один selectedSheet           ← КЛЮЧОВИЙ КРОК
    ↓
Показ статусу: "Обрано лист X · проігноровано N листів"
    ↓
Отримання актуальних правил ЄС через AI (окремий запит)
    ↓
Витяг рядків ТІЛЬКИ з selectedSheet
    ↓
isJunkRow() — фільтрація сміттєвих рядків
    ↓
normalizeItemName() + дедублікація
    ↓
analyzeItemsInBatches() → AI-аналіз батчами по 5 позицій
    (передається назва листа та дата в кожному userPrompt)
    ↓
Safety-фільтр: видалення товарів не з selectedSheet ← NEW
    ↓
Розрахунок митних платежів (JS, без AI)
    ↓
renderResults() → банер листа + метрики + таблиця
    ↓
Збереження в архів (з назвою листа)
```

### Лоадер (Loader)

Під час аналізу показується анімований термінальний лоадер з двома рядками статусу:
- Рядок 1: поточний крок (знайдено листів, обрано лист, кількість позицій, номер батчу)
- Рядок 2: деталі (причина вибору листа, кількість проігнорованих, статус батчу)

---

## 9. Обробка даних — вибір актуального листа

> **Головне правило v2.1:** система завжди аналізує **тільки один лист** — найактуальніший лист поточного збору. Жодного об'єднання даних між листами.

### 9.1. `parseSheetDate(sheetName, currentDate)`

Витягує дату з назви листа. Підтримувані формати:

| Формат | Приклад |
|--------|---------|
| `DD.MM.YYYY` | `06.05.2026` |
| `DD.MM` | `06.05` |
| `D.M` | `6.5` |
| `DD-MM` | `06-05` |
| `DD/MM` | `06/05` |
| `DD,MM` | `06,05` |
| З префіксом | `Сбор 06.05`, `Китай 06.05`, `06.05 контейнер` |

**Правила:**
- Якщо рік не вказано — використовується рік з `currentDate`
- Якщо отримана дата потрапляє в майбутнє — перевіряється попередній рік
- Якщо дата не знайдена — повертається `null`

### 9.2. `findDataHeader(rows)`

Знаходить рядок заголовків товарної таблиці. Перевіряє перші 10 рядків.

**Лист вважається товарним**, якщо є колонка назви товару:

| Колонки назви (обов'язково одна з) |
|------------------------------------|
| Номенклатура |
| Наименование |
| Назва / Название |
| Product |
| Item |

**І хоча б одна числова/товарна колонка:**

| Числові/товарні колонки |
|--------------------------|
| Кол-ва, Кол-во, Кількість |
| Вес, КГ, kg |
| Цена, Price |
| Закупка, Нетто |
| ТНВЭД, УКТЗЕД |
| Qty |

Повертає індекс рядка заголовків або `-1` якщо таблиця не знайдена.

### 9.3. `selectActualSheet(sheets, currentDate)`

Вибирає **один** актуальний лист за таким алгоритмом:

```
1. Для кожного листа: parseSheetDate() + findDataHeader()
2. Відокремлюємо листи з товарною таблицею (pool)
3. Серед pool — листи з датою (dated) та без (undated)
4. Шукаємо лист, дата якого == сьогодні → maxPriority
5. Якщо не знайдено → беремо найсвіжіший з dated де date <= currentDate
6. Якщо dated порожній → беремо перший з undated + warning
7. Повертаємо один selectedSheet + reason + ignoredSheets[]
```

**Результат функції:**

```js
{
  selected: { sheet, name, parsedDate, hasTable, headerIdx },
  reason: "Дата листа '06.05' збігається з поточною датою аналізу",
  ignored: ["29.04", "30.04", "Субстанції", "Архів", "Розрахунки"]
}
```

### 9.4. Що ігнорується автоматично

- Листи без товарної таблиці (немає колонки назви товару)
- Листи з датою, що пізніше поточної дати аналізу
- Старіші датовані листи (коли є більш свіжий)
- Листи-довідники, архіви, бази субстанцій — якщо в них немає товарної таблиці

### 9.5. Витяг рядків

Після вибору `selectedSheet` всі подальші операції виконуються **тільки з ним**:

- Пошук `headerIdx` та карти колонок
- Ітерація рядків з `headerIdx + 1`
- Визначення `nomIdx` (індекс колонки назви товару) — розширено: `номенкл|наименован|назван|product|item`
- Фільтрація через `isJunkRow()`
- Нормалізація через `normalizeItemName()`
- Дедублікація по нормалізованій назві

---

## 10. AI-аналіз та системний промпт

### Системний промпт

Містить **критичне правило джерела даних** (додано в v2.1):

```
КРИТИЧНЕ ПРАВИЛО ДЖЕРЕЛА ДАНИХ:

Ти отримуєш дані ЛИШЕ з одного заздалегідь відібраного актуального листа
поточного збору контейнера.

ЗАБОРОНЕНО:
- припускати наявність інших листів
- додавати товари зі старих збірок
- використовувати довідники, архіви або бази субстанцій
- об'єднувати дані з кількох листів
- відновлювати відсутні товари по пам'яті або контексту
- додавати товар, якщо його немає в переданому актуальному листі

Якщо у вибраному листі немає ціни, ваги, виробника або коду —
залиш поле порожнім/null/0, але не шукай дані в інших листах.
```

### User Prompt (на кожен батч)

```
Джерело: {sourceUrl}
Актуальний лист: "{selectedSheetName}" (дата: {selectedSheetDate})
Поточна дата аналізу: {currentDate}
Всі інші листи виключені до AI-аналізу. Аналізуй ЛИШЕ рядки нижче.
Партія {N} з {total} (позиції {start}–{end} з {allCount})
...
ДАНІ ПАРТІЇ:
{batchData}
```

### Батч-аналіз (`analyzeItemsInBatches`)

| Параметр | Значення |
|----------|---------|
| Розмір батчу | 5 позицій |
| `max_tokens` | 10 000 |
| Режим | JSON (без markdown) |
| Сигнатура | `analyzeItemsInBatches(rows, systemPrompt, sourceUrl, selectedSheetName, selectedSheetDate, currentDate)` |

Назва обраного листа, його дата та поточна дата аналізу передаються в кожен `userPrompt`.

### Формат відповіді AI

```json
{
  "items": [
    {
      "name": "ТОЧНА назва з таблиці",
      "uctzedCode": "10-значний код УКТЗЕД",
      "qtyKg": 500,
      "buyPricePerKg": 12.5,
      "dutyRatePercent": 5,
      "category": "АФІ (активна фармацевтична субстанція)",
      "applications": "Виробництво ліків",
      "hazardAnalysis": "Безпечний. Не ADR.",
      "storageRequirements": "Сухе місце, 15-25°C",
      "euChecks": [ { "item": "...", "status": "green|yellow|red", "note": "..." } ],
      "uaChecks": [ { "item": "...", "status": "green|yellow|red", "note": "..." } ],
      "risk": "Критичний|Середній|Низький",
      "riskNote": "причина ризику"
    }
  ],
  "borderDelay": "12-24 год",
  "criticalAlert": "...",
  "nctsList": ["пункт 1", "пункт 2"]
}
```

---

## 11. Safety-фільтр після AI

Після отримання відповіді від `analyzeItemsInBatches` застосовується **обов'язковий safety-фільтр**:

```js
// Будуємо множину дозволених назв з selectedSheet
const allowedNames = new Set(allDataRows.map(dr => normalizeItemName(назва_з_рядка)));

// Фільтруємо items від AI
parsed.items = parsed.items.filter(item => {
  const norm = normalizeItemName(item.name);
  // Точне співпадіння
  if (allowedNames.has(norm)) return true;
  // М'яке співпадіння: всі значущі слова (>3 символи) є в дозволеній назві
  for (const allowed of allowedNames) {
    const words = norm.split(/\s+/).filter(w => w.length > 3);
    if (words.length > 0 && words.every(w => allowed.includes(w))) return true;
    if (norm.length > 5 && allowed.includes(norm.slice(0, Math.floor(norm.length * 0.7)))) return true;
  }
  return false;
});
```

**Результат:** якщо AI додав товар, якого немає в `selectedSheet` — він видаляється з результату. Кількість видалених позицій логується в консоль як `[SAFETY FILTER]`.

---

## 12. Рендеринг результатів

Результати відображаються у 4 секціях (в v2.1 додано банер):

### Банер обраного листа (NEW)

Зелена інформаційна смуга вгорі результатів (показується якщо є `s.selectedSheetName`):

```
▶ ЛИСТ  "06.05"  06.05.2026
         Дата листа збігається з поточною датою аналізу
         Проігноровано (11): 29.04, 30.04, Субстанції, Архів, ...
```

### 01 · OVERVIEW — Фінансова зведена

Чотири метричні картки:

| Метрика | Формула | Тег |
|---------|---------|-----|
| Митна вартість | Σ (FOB × 1.10) | CIF |
| Мито | Σ (CIF × ставка%) | УКТЗЕД |
| ПДВ | Σ (CIF + Мито) × 20% | 20% |
| До сплати | Мито + ПДВ | ИТОГО |

### 02 · MANIFEST — Склад та план перевірок

Таблиця з горизонтальним скролом та 6 колонками:

1. **Товар / УКТЗЕД** — назва, код, категорія, застосування
2. **К-сть · Ціна** — кілограми, ціна за кг, ADR-бейдж (якщо є)
3. **Вартість · Платежі** — митна вартість, мито (зі ставкою), ПДВ
4. **Перевірки в ЄС** — світлофорний список перевірок + info-картки (небезпека, зберігання)
5. **Розмитнення UA** — аналогічний світлофорний список для України
6. **Ризик** — пілюля-бейдж із кольором (червоний/жовтий/зелений) + причина

### 03 · ADVISORY — Ризики та чекліст

- **Критичний фактор** — червоний алерт (якщо є небезпечні товари)
- **NCTS Phase 5 · Checklist** — зелений алерт зі списком кроків

---

## 13. Експорт звіту в XLSX

Функція `downloadReport()` генерує XLSX-файл із 4 аркушами через SheetJS:

| Аркуш | Зміст |
|-------|-------|
| **Зведена** | Метадані листа + фінансовий підсумок + кількість ризиків + NCTS чекліст |
| **Детальний аналіз** | Таблиця всіх товарів (15 колонок) з автофільтром та підсумковим рядком |
| **Перевірки ЄС** | Всі euChecks для кожного товару (товар, перевірка, статус-емодзі, коментар) |
| **Розмитнення UA** | Всі uaChecks аналогічно |

**Аркуш «Зведена» тепер містить (v2.1):**

| Рядок | Зміст |
|-------|-------|
| Дата звіту | Дата та час генерації |
| Аналізований лист | Назва обраного листа (напр. `06.05`) |
| Дата листа | Дата, витягнута з назви листа |
| Причина вибору листа | Пояснення алгоритму вибору |
| Проігноровано листів | Кількість ігнорованих листів |
| ... | Фінансова зведена, ризики, NCTS |

Ім'я файлу: `LOGI-ANALYZER_Report_YYYY-MM-DD.xlsx`

---

## 14. Архів перевірок

- Зберігається в `localStorage` під ключем `lap_archive`
- Максимум **50 записів** (FIFO)
- Кожен запис містить: URL/назву джерела, дату, **назву обраного листа** (v2.1), кількість позицій, загальну суму, рівень ризику (hasHigh/hasMed), повні дані аналізу
- При кліку по запису — відкривається сторінка «Новий аналіз» із збереженими результатами
- Індикація типу джерела: 📄 Файл, 🔗 Google Sheets, 📊 Аналіз

---

## 15. База знань ЗЕД

Містить 8+ тематичних карток (`zedTopics`), кожна із секціями та рядками довідкових даних:

| # | Тема | Іконка |
|---|------|--------|
| 1 | Фармацевтика · АФІ · Імпорт | 💊 |
| 2 | REACH · CLP · SDS · 2025 | 🧪 |
| 3 | GMP · EudraLex · ICH Q7 | 🏭 |
| 4 | Транзит ЄС · NCTS Phase 5 | 🇪🇺 |
| 5 | ADR · Небезпечні вантажі | ☢️ |
| 6 | Нові регуляторні вимоги 2025–2026 | 📋 |
| 7 | Митна вартість · Інкотермс 2020 | 📊 |
| 8 | Митниця України · 2025–2026 | 🛃 |
| 9 | Санкції · Dual-Use · Export Control | ⚖️ |

---

## 16. Теми оформлення

Додаток підтримує **дві теми** з плавним перемиканням (transition 0.3s):

### Темна тема (за замовчуванням)
- Фон: `#0a0b0d`
- Поверхні: `#111317` → `#1b1f25`
- Текст: `#e8ebef` (основний), `#9ba3ac` (вторинний)
- Акцент: `#2cb878` (зелений)

### Світла тема (`.theme-light`)
- Фон: `#f3f5f8`
- Поверхні: `#ffffff`
- Текст: `#111827` (основний), `#374151` (вторинний)
- Акцент: `#059669` (зелений, темніший)

Перемикання: кнопка з анімованими SVG-іконками місяця/сонця (3D-ефект translate + rotate). Вибір зберігається в `localStorage` (`lap_theme`).

---

## 17. Адаптивність (Responsive)

Breakpoint: `@media (max-width: 900px)`

Зміни:
- Topbar: переходить на 2-колонковий grid, мета-секція (тема, API-чіп) ховається
- Навігація: розтягується на всю ширину
- Command Deck: зменшені padding'и
- Input Row: колонки стають вертикальними (flex-direction: column)
- Кнопка «Запустити аналіз»: займає 100% ширини
- Keybar: одна колонка

---

## 18. Зберігання даних

Усі дані зберігаються в `localStorage` браузера:

| Ключ | Тип | Призначення |
|------|-----|-------------|
| `lap_provider` | string | Активний AI-провайдер (`openai`, `gemini`, `claude`, `openrouter`) |
| `lap_provider_keys` | JSON object | API-ключі для кожного провайдера |
| `lap_key_history` | JSON array | Історія змін ключів (до 30 записів) |
| `lap_archive` | JSON array | Архів аналізів (до 50 записів), включно з `selectedSheetName` |
| `lap_theme` | string | Тема оформлення (`light` або відсутнє = dark) |

---

## 19. Зовнішні залежності

| Ресурс | URL | Завантаження |
|--------|-----|-------------|
| Google Fonts | `fonts.googleapis.com` | При завантаженні сторінки |
| SheetJS (XLSX) | `cdn.sheetjs.com/xlsx-0.20.1/...` | Динамічно при першому використанні |
| OpenAI API | `api.openai.com` | При аналізі |
| Google Gemini API | `generativelanguage.googleapis.com` | При аналізі |
| Anthropic API | `api.anthropic.com` | При аналізі |
| OpenRouter API | `openrouter.ai` | При аналізі |
| Google Sheets Export | `docs.google.com` | При аналізі Google Sheets |

---

## 20. Обмеження та відомі особливості

- **Немає бекенду** — API-ключі зберігаються в localStorage браузера (без шифрування). Це зручно, але потенційно небезпечно на спільних комп'ютерах.
- **CORS для Claude** — використовується нестандартний заголовок `anthropic-dangerous-direct-browser-access: true` для обходу CORS-обмежень Anthropic API при прямому виклику з браузера.
- **Google Sheets** — таблиця повинна бути публічною для доступу без автентифікації.
- **Батч-ліміт** — по 5 позицій на AI-запит; для великих маніфестів (100+ позицій) це може зайняти значний час.
- **Вибір листа** — якщо жоден лист не містить товарної таблиці (немає колонок `Номенклатура` + числових колонок) — аналіз завершується помилкою. Перевірте структуру файлу.
- **Парсинг дати листа** — ґрунтується виключно на назві листа (дата редагування SheetJS не використовується, оскільки недоступна в XLSX-export). Якщо назва листа не містить дати — лист потрапляє в категорію `undated`.
- **Safety-фільтр** — використовує м'яке зіставлення назв, тому у крайніх випадках може вилучити товар із незвичайним форматуванням назви в AI-відповіді. Перевіряйте лог консолі `[SAFETY FILTER]`.
- **Немає офлайн-режиму** — потребує інтернет-з'єднання для AI API та Google Fonts.
- **Немає мультимовності** — інтерфейс виключно українською мовою (з елементами англійської в технічних секціях).
- **Один файл** — увесь код в одному HTML-файлі (~4600 рядків), що ускладнює підтримку при подальшому розвитку.

---

## 21. Структура коду

### CSS (~2400 рядків)

Секції: CSS змінні → Reset → Body + Ambient → Topbar → Nav → Theme → Pages → Deck → API Key Card → Key Modal → Tables → Metrics → Alerts → Status pills → Loader → Knowledge Base → Footer → Responsive.

### HTML (~190 рядків)

Структура: `<header.topbar>` → `<main#page-new>` (API Key Card + Command Deck + Results) → `<main#page-arch>` → `<main#page-zed>` → `<footer>` → Key Management Modal.

### JavaScript (~2200 рядків)

| Блок | Функціонал |
|------|------------|
| Provider Management | Константи провайдерів, збереження/завантаження ключів |
| Key Modal | Модальне вікно управління ключами |
| `callAI()` | Універсальний AI-caller для 4 провайдерів |
| `showPage()` | Навігація між сторінками |
| `showLoader / setLoaderStep` | Анімований лоадер з двома рядками статусу |
| `runAudit()` | Головна функція аналізу (~700 рядків) |
| `parseSheetDate()` | Парсинг дати з назви листа (всі формати) |
| `findDataHeader()` | Пошук рядка заголовків товарної таблиці |
| `selectActualSheet()` | Вибір одного актуального листа ← NEW |
| `isJunkRow()` | Фільтрація сміттєвих рядків (коментарі, нотатки) |
| `normalizeItemName()` | Нормалізація назви товару для дедублікації |
| Safety Filter | Видалення з AI-відповіді товарів не з selectedSheet ← NEW |
| `formatTableForAI()` | Форматування таблиці для AI |
| `renderChecksList()` | Рендеринг світлофорного списку перевірок |
| `downloadReport()` | Генерація та завантаження XLSX (з метаданими листа) |
| `renderResults()` | Побудова HTML результатів (з банером листа) |
| `renderArchive()` | Відображення архіву (з назвою листа) |
| `zedTopics[]` | Дані бази знань (9 тематичних блоків) |
| `renderZed / openZed / closeZed` | Рендеринг та навігація бази знань |
| `handleFileUpload()` | Обробка завантаження файлів |
| `loadXLSX()` | Динамічне завантаження SheetJS з CDN |
| `analyzeItemsInBatches()` | Батч-аналіз через AI (приймає контекст листа) |
| Clock | Годинник UTC у topbar |

---

## 22. Changelog v2.1

### Виправлено

**Критична проблема:** при аналізі файлу з кількома листами система збирала товари з усіх листів одночасно (старі збірки, бази субстанцій, архіви), що призводило до появи зайвих товарів у результаті.

### Що змінено

| # | Що | Де |
|---|----|----|
| 1 | Додано `parseSheetDate()` з підтримкою 10+ форматів дат | `runAudit()` |
| 2 | Розширено `findDataHeader()`: підтримка Наименование, Product, Item тощо | `runAudit()` |
| 3 | Додано `selectActualSheet()` — вибір одного актуального листа | `runAudit()` |
| 4 | Pipeline перероблено: `orderedSheets = [selectedMeta]` (масив з одного листа) | `runAudit()` |
| 5 | Системний промпт AI: видалено інструкцію "читай усі листи", додано блок КРИТИЧНЕ ПРАВИЛО | `systemPrompt` |
| 6 | `userPrompt` кожного батчу: вказується назва листа, дата, поточна дата аналізу | `analyzeItemsInBatches()` |
| 7 | `analyzeItemsInBatches()`: нова сигнатура з `selectedSheetName`, `selectedSheetDate`, `currentDate` | `analyzeItemsInBatches()` |
| 8 | Додано safety-фільтр після AI: видалення товарів не з `selectedSheet` | після `analyzeItemsInBatches()` |
| 9 | `renderResults()`: зелений банер з назвою листа, причиною вибору, переліком ігнорованих | `renderResults()` |
| 10 | `parsed.summary`: додано `selectedSheetName`, `selectedSheetDate`, `sheetReason`, `ignoredSheets` | `runAudit()` |
| 11 | `downloadReport()`: аркуш «Зведена» містить метадані листа | `downloadReport()` |
| 12 | Архів (`lap_archive`): кожен запис зберігає `selectedSheetName` | `renderResults()` |
| 13 | Лоадер: показує "Знайдено листів: N · Обрано: X · Ігнорується: M листів" | `setLoaderStep()` |

### Acceptance criteria (підтверджено)

- ✅ Файл з листами `29.04`, `30.04`, `06.05`, `Субстанції`, `Архів` → аналізується тільки `06.05`
- ✅ Якщо сьогодні `06.05.2026` і є лист `06.05` → він вибирається автоматично
- ✅ Якщо листа `06.05` нема, але є `05.05` та `02.05` → вибирається `05.05`
- ✅ Товари з `Субстанції`, `29.04`, `30.04` не потрапляють у результат
- ✅ AI не отримує у prompt дані з інших листів
- ✅ В UI видно, який лист обрано та чому
- ✅ Safety-фільтр видаляє «фантомні» товари з AI-відповіді

---

*Документація актуальна для `index.html` проєкту LOGI-ANALYZER PRO v2.1 · © 2026*
