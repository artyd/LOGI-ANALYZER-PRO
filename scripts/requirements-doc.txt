# Залежності для scripts/doc_extract.py (конвертація документів → чистий текст + витяг полів)
# Базові (конвертація тексту) — зазвичай уже встановлені:
PyMuPDF>=1.24      # PDF (fitz)
python-docx>=1.1   # DOCX
openpyxl>=3.1      # XLSX
Pillow>=10.0       # зображення / рендер сторінок

# OCR для сканів (опційно). ТАКОЖ потрібен СИСТЕМНИЙ tesseract:
#   Windows: https://github.com/UB-Mannheim/tesseract (додати мовні пакети eng, chi_sim, ukr)
#   потім:   python scripts/doc_extract.py <файл> --lang eng+chi_sim+ukr
pytesseract>=0.3.10
